{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window — minimal permissions, no filesystem or dialog access required",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
